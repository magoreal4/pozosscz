const path = require('path');
const { defineConfig } = require('vite')
module.exports = defineConfig({
    build: {
        minify: false,
        // outDir: path.resolve(__dirname, 'static/js'),
        // filename: 'bundle.js',
        // sourcemap: 'hidden',
        rollupOptions: {
            output: {
                entryFileNames: 'kute-magoreal.js',
            //     // assetFileNames: `[name].[ext]`,
              },
            input: './src/index-custom.js',
            // format: 'umd',
        }
    }
});

